
import PricingSection from "@/components/PricingSection";

const PricingSectionWrapper = () => {
  return (
    <section id="pricing" className="relative">
      <PricingSection />
    </section>
  );
};

export default PricingSectionWrapper;
